# Tuples are like lists but cannot be changed (immutable)

person = ("Amaan", 25, 1.68)
print(person)

# Access elements by index
print(person[0])    # 'Amaan'

# Tuples are immutable
# person[1] = 30  # This will cause an error!
